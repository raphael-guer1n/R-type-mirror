name: "CI - Build"

on:
  pull_request:
  push:
    branches:
      - main
      - dev

jobs:
  build-and-test:
    name: "Build in Epitech Docker"
    container: epitechcontent/epitest-docker:latest
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          export DEBIAN_FRONTEND=noninteractive
          apt-get update -y
          apt-get install -y --no-install-recommends \
            curl zip unzip tar pkg-config \
            build-essential cmake ninja-build git \
            autoconf automake libtool \
            libasound2-dev libpulse-dev \
            libx11-dev libxext-dev libxrandr-dev libxcursor-dev libxi-dev \
            libgl1-mesa-dev libegl1-mesa-dev libwayland-dev wayland-protocols
          apt-get clean
          rm -rf /var/lib/apt/lists/*

      - name: Remove ALSA feature from vcpkg.json
        run: |
          # Remove "alsa" from any features array
          sed -i 's/"alsa"\s*,\?//g' vcpkg.json
          # Remove trailing commas before closing brackets
          sed -i 's/,\s*]/]/g' vcpkg.json

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: vcpkg/installed
          key: ${{ runner.os }}-vcpkg-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            ${{ runner.os }}-vcpkg-

      - name: Setup vcpkg
        run: |
          if [ ! -d "vcpkg" ]; then
            git clone https://github.com/microsoft/vcpkg.git
          fi
          export VCPKG_FORCE_SYSTEM_BINARIES=1
          ./vcpkg/bootstrap-vcpkg.sh

      - name: Build project (including tests)
        run: |
          chmod +x ./build.sh
          ./build.sh